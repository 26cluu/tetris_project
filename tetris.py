# class O_Block(Tetromino):
#     def __init__(self):
#         self.pos = [Vector2(0, 0), Vector2(0, 1), Vector2(1, 0), Vector2(1,1)]  
        
#     def spawn_block(self):
#         for block in self.pos:
#             print(block.x, block.y)
#             map[round(block.x)][round(block.y)] = 1

# class L_Block(Tetromino):
#     def __init__(self):
#         self.pos = [Vector2(0, 0), Vector2(1, 0), Vector2(2, 0), Vector2(2,1)]
                        
#     def spawn_block(self):
#         for block in self.pos:
#             print(block.x, block.y)
#             map[round(block.x)][round(block.y)] = 2

# class J_Block(Tetromino):
#     def __init__(self):
#         self.pos = [Vector2(0, 1), Vector2(1, 1), Vector2(2, 1), Vector2(2,0)]
                        
#     def spawn_block(self):
#         for block in self.pos:
#             print(block.x, block.y)
#             map[round(block.x)][round(block.y)] = 3

# class I_Block(Tetromino):
#     def __init__(self):
#         self.pos = [Vector2(0, 0), Vector2(1, 0), Vector2(2, 0), Vector2(3,0)]
                        
#     def spawn_block(self):
#         for block in self.pos:
#             print(block.x, block.y)
#             map[round(block.x)][round(block.y)] = 4

# class T_Block(Tetromino):
#     def __init__(self):
#         self.pos = [Vector2(0, 0), Vector2(0, 1), Vector2(0, 2), Vector2(1, 1)]
                        
#     def spawn_block(self):
#         for block in self.pos:
#             print(block.x, block.y)
#             map[round(block.x)][round(block.y)] = 5

# class S_Block(Tetromino):
#     def __init__(self):
#         self.pos = [Vector2(0, 1), Vector2(0, 2), Vector2(1, 0), Vector2(1, 1)]
                        
#     def spawn_block(self):
#         for block in self.pos:
#             print(block.x, block.y)
#             map[round(block.x)][round(block.y)] = 6

# class Z_Block(Tetromino):
#     def __init__(self):
#         self.pos = [Vector2(0, 0), Vector2(0, 1), Vector2(1, 1), Vector2(1, 2)]
                        
#     def spawn_block(self):
#         for block in self.pos:
#             print(block.x, block.y)
#             map[round(block.x)][round(block.y)] = 7  

# oblock = O_Block()
# oblock.spawn_block()

# lblock = L_Block()
# lblock.spawn_block()

# jblock = J_Block()
# jblock.spawn_block()

# iblock = I_Block()
# iblock.spawn_block()

# tblock = T_Block()
# tblock.spawn_block()

# sblock = S_Block()
# sblock.spawn_block()

# zblock = Z_Block()
# zblock.spawn_block()

